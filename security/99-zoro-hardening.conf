# ═══════════════════════════════════════════════════════════════
# Zoro Linux 10 — Kernel Security Hardening
# /etc/sysctl.d/99-zoro-hardening.conf
# ═══════════════════════════════════════════════════════════════
# "If I die here, then I'm a man that could only make it this far."
# Applied without breaking normal use cases.

# ── Kernel Hardening ─────────────────────────────────────────
# Restrict dmesg to root only
kernel.dmesg_restrict = 1

# Restrict kernel pointer exposure
kernel.kptr_restrict = 2

# Protect hard links from ToCToU race conditions
fs.protected_hardlinks = 1

# Protect symbolic links from ToCToU race conditions
fs.protected_symlinks = 1

# Restrict access to kernel logs
kernel.printk = 3 4 1 3

# Restrict BPF JIT to root
kernel.unprivileged_bpf_disabled = 1

# Enable BPF JIT hardening
net.core.bpf_jit_harden = 2

# Restrict perf events to root
kernel.perf_event_paranoid = 3

# ── Network Hardening ───────────────────────────────────────
# Enable reverse path filtering (anti-spoofing)
net.ipv4.conf.all.rp_filter = 1
net.ipv4.conf.default.rp_filter = 1

# Enable TCP SYN cookies (SYN flood protection)
net.ipv4.tcp_syncookies = 1

# Disable IPv6 router advertisements
net.ipv6.conf.all.accept_ra = 0

# Disable ICMP redirect acceptance
net.ipv4.conf.all.accept_redirects = 0
net.ipv4.conf.default.accept_redirects = 0
net.ipv6.conf.all.accept_redirects = 0
net.ipv6.conf.default.accept_redirects = 0

# Disable ICMP redirect sending
net.ipv4.conf.all.send_redirects = 0
net.ipv4.conf.default.send_redirects = 0

# Disable source routing
net.ipv4.conf.all.accept_source_route = 0
net.ipv4.conf.default.accept_source_route = 0
net.ipv6.conf.all.accept_source_route = 0
net.ipv6.conf.default.accept_source_route = 0

# Log martian packets
net.ipv4.conf.all.log_martians = 1
net.ipv4.conf.default.log_martians = 1

# Enable TCP timestamps
net.ipv4.tcp_timestamps = 1

# ── Memory Hardening ────────────────────────────────────────
# Restrict core dumps
fs.suid_dumpable = 0

# Enable address space layout randomization (ASLR)
kernel.randomize_va_space = 2

# Restrict access to /proc/pid/maps
# (helps prevent ASLR bypass attacks)
kernel.maps_protect = 1
